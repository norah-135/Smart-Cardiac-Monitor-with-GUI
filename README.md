# Smart-Cardiac-Monitor-with-GUI
An IoT Heart Rate Monitor designed for athletic recovery optimization. Featuring a real-time Blynk GUI, it provides instant visual feedback to prevent premature training restarts; alerting athletes to wait if the status is Red (Alarm) and proceed only when it turns Green (Normal).
